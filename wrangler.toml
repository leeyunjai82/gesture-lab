# Cloudflare Workers 정적 배포 — 파이보 랩과 같은 방식
# 배포: npx wrangler deploy
# 시험 기간 잠금: npx wrangler secret put BASIC_USER / BASIC_PASS (둘 다 있어야 켜진다)
name = "gesture-lab"
main = "src/index.js"
compatibility_date = "2026-08-01"

[assets]
directory = "."
binding = "ASSETS"
run_worker_first = true
